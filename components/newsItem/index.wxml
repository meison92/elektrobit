<view class="news-item" bindtap="tapProductDetail">
	<view class="f14 date">
		<view>{{data.year}}年</view>
		<view>{{data.month}}月{{data.date}}日</view>
	</view>
	<view class="f14 content green">{{data.title}}</view>
</view>